# Local development environment variables
PORT=3001
JWT_SECRET=change-me-local
JWT_EXPIRES_IN=7d

# Local PostgreSQL connection (fallback if DATABASE_URL not set)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=chessarena
DB_USER=postgres
DB_PASSWORD=postgres

# Optional: use a full connection string instead of DB_* vars
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/chessarena
